<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Welcome to Huts4u</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f9;
      margin: 0;
      padding: 0;
    }

    .card {
      max-width: 640px;
      margin: 25px auto;
      background: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    }

    .header {
      background: linear-gradient(90deg, #6a0dad, #8a2be2);
      padding: 25px;
      text-align: center;
      color: #ffffff;
    }

    .logo {
      height: 60px;
      margin-bottom: 12px;
    }

    .title {
      font-size: 24px;
      font-weight: bold;
    }

    .body {
      padding: 28px;
      color: #3a3a4d;
      font-size: 16px;
      line-height: 1.7;
    }

    .credentials {
      background: #f9f5ff;
      border: 1px dashed #d7b2ff;
      padding: 16px;
      margin-top: 18px;
      border-radius: 10px;
    }

    .label {
      font-weight: bold;
      color: #6a0dad;
      font-size: 15px;
      margin-bottom: 8px;
    }

    .btn {
      display: inline-block;
      margin-top: 25px;
      background: #6a0dad;
      color: #ffffff !important;
      padding: 12px 25px;
      border-radius: 8px;
      text-decoration: none;
      font-size: 16px;
      font-weight: bold;
      letter-spacing: 0.5px;
    }

    .btn:hover {
      background: #5a0ca0;
    }

    .footer {
      padding: 18px;
      text-align: center;
      font-size: 13px;
      color: #8a8aa8;
      background: #fbfbff;
      border-top: 1px solid #eee;
    }
  </style>
</head>

<body>
  <div class="card">
    <div class="header">

      <% if (logoUrl) { %>
        <img src="<%= logoUrl %>" alt="Huts4u" class="logo" />
      <% } else { %>
        <div class="title">Huts4u</div>
      <% } %>

      <div class="title">Welcome to Huts4u!</div>
    </div>

    <div class="body">
      <p>Hi <b><%= name %></b>,</p>

      <p>Welcome to the Huts4u family! Your account has been successfully created, and you’re all set to explore our platform and book your perfect stay.</p>

      <div class="credentials">
        <p class="label">Your Login Credentials</p>
        <p>Email: <b><%= email %></b></p>
        <p>Password: <b><%= password %></b></p>
      </div>

      <a class="btn" href="<%= loginUrl || '#' %>">Login to Huts4u</a>

      <p style="margin-top: 22px;">If this account was not created by you, please contact our support team immediately.</p>
    </div>

    <div class="footer">
      © <%= new Date().getFullYear() %> Huts4u • All rights reserved.
    </div>
  </div>
</body>
</html>
